# Coder: implement story

```
Your assignment is ready: read handoff/planner_handoff.md (it is the detailed
Story {{STORY_NN}}).

Implement it following your persona's workflow: understand → plan → build →
self-verify against every acceptance criterion → report.

Git: {{GIT_MODE_INSTRUCTIONS}}

Write handoff/coder_report.md in your persona's format — including evidence for
each acceptance criterion and notes telling the verifier exactly how to exercise
the feature. End with your sentinel.
```

`{{GIT_MODE_INSTRUCTIONS}}` is filled by smashhh as either:
- `branch-per-story` → "Create branch story-{{STORY_NN}}-<short-slug> from main and do all work there. Do not merge."
- `fast` → "Work on main, but do not commit — the verifier commits after a PASS."
